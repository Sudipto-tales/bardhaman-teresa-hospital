<?php

/**
 * The topbar mount point. Client-rendered for the same reason as the sidebar —
 * `core/layout.js` replaces this node outright — and additionally because the
 * global search it carries reads the same in-page collections the rest of the
 * screen does.
 */
?>
            <div id="topbar"></div>
